  {
    "id": "sshcontroller",
    "kind": "ReplicationController",
    "apiVersion": "v1beta1",
    "desiredState": {
      "replicas": 3,
      "replicaSelector": {"name": "ssh"},
      "podTemplate": {
        "desiredState": {
           "manifest": {
             "version": "v1beta1",
             "id": "ssh",
             "containers": [{
               "name": "ubuntu-sshd",
               "image": "rastasheep/ubuntu-sshd",
               "ports": [{"name": "ssh", "containerPort": 22}]
             }]
           }
         },
         "labels": {"name": "ssh"}
        }},
    "labels": {"name": "ssh"}
  }
